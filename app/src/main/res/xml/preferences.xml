<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:app="http://schemas.android.com/apk/res-auto">

    <SwitchPreferenceCompat
        app:key="authenticated_user"
        app:title="User is already authenticated" />

    <SwitchPreferenceCompat
        app:key="login_with_xoom"
        app:title="User login using xoom" />

    <SwitchPreferenceCompat
        app:key="login_with_paypal"
        app:title="User login using PayPal" />

</PreferenceScreen>